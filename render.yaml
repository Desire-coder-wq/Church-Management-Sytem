services:
  - type: web
    name: church-backend-api
    runtime: node
    rootDir: Backend         
    buildCommand: npm install
    startCommand: npm start
    buildFilter:
      paths:
        - Backend/**         