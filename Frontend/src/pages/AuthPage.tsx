import {FormEvent,useState} from 'react';import {useNavigate} from 'react-router-dom';import {api,getError} from '../api/client';import {useAuthStore} from '../stores/auth-store';import {Alert} from '../components/Alert';export function AuthPage({signup=false}:{signup?:boolean}){const nav=useNavigate(),setSession=useAuthStore(s=>s.setSession);const[form,setForm]=useState({fullName:'',email:'',password:''}),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();if(signup&&form.fullName.trim().length<2)return setError('Enter your full name.');if(form.password.length<8)return setError('Password must have at least 8 characters.');setBusy(true);try{const r=await api.post(signup?'/auth/signup':'/auth/login',signup?form:{email:form.email,password:form.password});setSession(r.data.accessToken,r.data.user);nav('/dashboard')}catch(e){setError(getError(e))}finally{setBusy(false)}}return <main className="grid min-h-screen place-items-center p-5"><form onSubmit={submit} className="card w-full max-w-md"><p className="font-semibold text-gold">CHURCH PLEDGE MANAGER</p><h1 className="mt-2 text-2xl font-bold">{signup?'Create your workspace':'Welcome back'}</h1>{error&&<Alert message={error}/>} {signup&&<label className="mt-5 block text-sm">Full name<input className="field" required value={form.fullName} onChange={e=>setForm({...form,fullName:e.target.value})}/></label>}<label className="mt-4 block text-sm">Email<input className="field" type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label className="mt-4 block text-sm">Password<input className="field" type="password" minLength={8} required value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label><button disabled={busy} className="btn primary mt-6 w-full">{busy?'Please wait…':signup?'Create account':'Sign in'}</button><p className="mt-5 text-center text-sm text-muted"><a className="text-navy" href={signup?'/login':'/signup'}>{signup?'Sign in instead':'Create an account'}</a></p></form></main>}
