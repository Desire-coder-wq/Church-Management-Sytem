import {FormEvent,useEffect,useState} from 'react';import {api,getError} from '../api/client';import {Alert} from '../components/Alert';import {ugx} from '../utils/currency';export function ManagementPage({title,path,fields}:{title:string;path:string;fields:string[]}){const[rows,setRows]=useState<any[]>([]),[form,setForm]=useState<any>({}),[open,setOpen]=useState(false),[error,setError]=useState(''),[success,setSuccess]=useState('');const load=()=>api.get(path).then(r=>setRows(r.data.rows??r.data)).catch(e=>setError(getError(e)));useEffect(()=>{void load()},[path]);async function submit(e:FormEvent){e.preventDefault();try{await api.post(path,{...form,amount:Number(form.amount),targetAmount:Number(form.targetAmount),paymentDate:form.paymentDate||new Date().toISOString(),idempotencyKey:form.idempotencyKey||crypto.randomUUID()});setSuccess(`${title.slice(0,-1)} saved successfully.`);setOpen(false);setForm({});load()}catch(e){setError(getError(e))}}return <><div className="flex justify-between"><div><h1 className="text-2xl font-bold">{title}</h1><p className="text-sm text-muted">Live records from Neon.</p></div>{fields.length>0&&<button className="btn primary" onClick={()=>setOpen(!open)}>Add</button>}</div>{error&&<Alert message={error}/>} {success&&<Alert message={success} variant="success"/>}{open&&<form onSubmit={submit} className="card mt-6 grid gap-4 md:grid-cols-2">{fields.map(field=><label key={field} className="text-sm">{field}<input className="field" required type={field.includes('Date')?'date':field==='amount'||field==='targetAmount'?'number':'text'} value={form[field]??''} onChange={e=>setForm({...form,[field]:e.target.value})}/></label>)}<button className="btn primary md:col-span-2">Save</button></form>}<div className="card mt-6 overflow-auto"><table className="w-full min-w-[600px] text-left text-sm"><thead><tr><th className="p-3">Record</th><th>Details</th><th>Status</th></tr></thead><tbody>{rows.map(x=><tr className="border-t border-line" key={x.id}><td className="p-3">{x.fullName??x.name??x.member?.fullName??'Collection'}</td><td>{x.phone??x.campaign?.name??(x.amount?ugx(x.amount):x.message??'—')}</td><td>{x.status??'—'}</td></tr>)}</tbody></table></div></>}
