import axios from 'axios';import {useAuthStore} from '../stores/auth-store';export const api=axios.create({baseURL:import.meta.env.VITE_API_URL??'http://localhost:3000/api'});api.interceptors.request.use(c=>{const token=useAuthStore.getState().token;if(token)c.headers.Authorization=`Bearer ${token}`;return c});export const getError=(e:any)=>e?.response?.data?.message?.join?.(', ')??e?.response?.data?.message??'Something went wrong. Please try again.';
