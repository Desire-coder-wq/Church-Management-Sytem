import {Module} from '@nestjs/common'; import {ConfigModule} from '@nestjs/config'; import {JwtModule} from '@nestjs/jwt'; import {PassportModule} from '@nestjs/passport'; import {ThrottlerModule} from '@nestjs/throttler'; import {ApiController,AuthController} from './controllers'; import {PrismaService,AuthService,ChurchService,SmsService,JwtStrategy} from './services';
@Module({imports:[ConfigModule.forRoot({isGlobal:true}),PassportModule,JwtModule.register({secret:process.env.JWT_SECRET||'development-only-change-me',signOptions:{expiresIn:'8h'}}),ThrottlerModule.forRoot([{ttl:60000,limit:100}])],controllers:[AuthController,ApiController],providers:[PrismaService,AuthService,ChurchService,SmsService,JwtStrategy]}) export class AppModule{}
